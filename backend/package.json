{
  "name": "jaarvi-backend",
  "version": "1.0.0",
  "description": "Backend API for Jaarvi travel planning assistant",
  "main": "dist/index.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "lint": "eslint src --ext .ts",
    "lint:fix": "eslint src --ext .ts --fix",
    "format": "prettier --write \"src/**/*.ts\"",
    "typecheck": "tsc --noEmit",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "prisma:migrate:create": "prisma migrate dev --create-only",
    "prisma:migrate:deploy": "prisma migrate deploy",
    "prisma:studio": "prisma studio",
    "prisma:format": "prisma format",
    "seed": "npx ts-node prisma/seed.ts",
    "seed:activity-types": "npx ts-node -e \"const { PrismaClient } = require('@prisma/client'); const { seedActivityTypes } = require('./prisma/seeds/activityTypes'); seedActivityTypes(new PrismaClient()).then(() => process.exit(0))\"",
    "seed:destinations": "npx ts-node -e \"const { PrismaClient } = require('@prisma/client'); const { seedDestinations } = require('./prisma/seeds/destinations'); seedDestinations(new PrismaClient()).then(() => process.exit(0))\"",
    "seed:users": "npx ts-node -e \"const { PrismaClient } = require('@prisma/client'); const { seedTestUsers } = require('./prisma/seeds/users'); seedTestUsers(new PrismaClient()).then(() => process.exit(0))\"",
    "seed:trips": "npx ts-node -e \"const { PrismaClient } = require('@prisma/client'); const { seedSampleTrips } = require('./prisma/seeds/sampleTrips'); seedSampleTrips(new PrismaClient()).then(() => process.exit(0))\""
  },
  "prisma": {
    "seed": "npx ts-node prisma/seed.ts"
  },
  "keywords": [
    "jaarvi",
    "travel",
    "api",
    "express",
    "typescript"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "5.22.0",
    "argon2": "^0.44.0",
    "bcryptjs": "2.4.3",
    "cors": "2.8.5",
    "dotenv": "16.4.5",
    "express": "4.21.1",
    "helmet": "8.0.0",
    "jsonwebtoken": "9.0.2",
    "winston": "3.17.0"
  },
  "devDependencies": {
    "@types/bcryptjs": "2.4.6",
    "@types/cors": "2.8.17",
    "@types/express": "4.17.21",
    "@types/jest": "29.5.14",
    "@types/jsonwebtoken": "9.0.7",
    "@types/node": "20.17.6",
    "@typescript-eslint/eslint-plugin": "6.21.0",
    "@typescript-eslint/parser": "6.21.0",
    "eslint": "8.57.1",
    "jest": "29.7.0",
    "prettier": "3.3.3",
    "prisma": "5.22.0",
    "ts-jest": "29.2.5",
    "ts-node": "^10.9.2",
    "ts-node-dev": "2.0.0",
    "typescript": "5.6.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
