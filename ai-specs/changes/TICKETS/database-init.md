# Database Initialization - Jaarvi

## Ticket Info

| Field | Value |
|-------|-------|
| **Type** | Task |
| **Priority** | High |
| **Depends On** | `TICKETS/backend-scaffolding.md` |
| **Blocks** | All backend feature tickets |

---

## [Original]

> Initialize the PostgreSQL database with Prisma ORM, create all tables according to the database specification, and populate seed data for development.

---

## [Enhanced]

### User Story

**As a** development team member  
**I want** a fully configured PostgreSQL database with all Jaarvi tables and seed data  
**So that** I can start developing features that require data persistence

### Description

This task sets up the complete database infrastructure for the Jaarvi backend, including:

- Docker configuration for local PostgreSQL
- Prisma schema with all 27 models from the database specification
- Initial migration to create all tables
- Seed data for development and testing (activity types, destinations, test users, sample trips)
- Database connection verification

### Prerequisites

- `backend-scaffolding.md` must be completed
- Node.js and npm installed
- Docker installed and running

---

## Technology Stack

| Component | Technology | Version |
|-----------|------------|---------|
| **Database** | PostgreSQL | 15.x |
| **ORM** | Prisma | 5.x |
| **Container** | Docker Compose | 3.8+ |
| **Password Hashing** | argon2 | 0.31.x |

---

## Files to Create

### Docker Configuration (1 file)

| File | Purpose |
|------|---------|
| `backend/docker-compose.yml` | PostgreSQL container for local development |

### Prisma Files (6 files)

| File | Purpose |
|------|---------|
| `backend/prisma/schema.prisma` | Complete database schema with 27 models |
| `backend/prisma/seed.ts` | Main seed orchestrator |
| `backend/prisma/seeds/activityTypes.ts` | Activity type configuration seeds |
| `backend/prisma/seeds/destinations.ts` | Countries and cities with coverage seeds |
| `backend/prisma/seeds/users.ts` | Test user seeds (development only) |
| `backend/prisma/seeds/sampleTrips.ts` | Sample trip seeds (development only) |

### Migration Files (auto-generated)

| File | Purpose |
|------|---------|
| `backend/prisma/migrations/[timestamp]_init/migration.sql` | Initial migration SQL (auto-generated by Prisma) |

---

## Docker Configuration

### `docker-compose.yml`

```yaml
version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: jaarvi-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: jaarvi_dev
      POSTGRES_PASSWORD: jaarvi_local_dev_2024
      POSTGRES_DB: jaarvi_dev
    ports:
      - "5432:5432"
    volumes:
      - jaarvi_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U jaarvi_dev -d jaarvi_dev"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  jaarvi_postgres_data:
    driver: local
```

### Database Connection String

```
DATABASE_URL="postgresql://jaarvi_dev:jaarvi_local_dev_2024@localhost:5432/jaarvi_dev"
```

---

## Prisma Schema Models (27 models)

Complete list of models to implement in `schema.prisma`:

### Authentication & Identity (7 models)

| Model | Purpose | Key Fields |
|-------|---------|------------|
| `User` | User identity | id (UUID), email (unique), displayName, status, emailVerifiedAt |
| `UserCredential` | Password storage | userId (unique FK), passwordHash, passwordAlgo |
| `Session` | Active sessions | userId FK, deviceName, userAgent, ipAddress, expiresAt, revokedAt |
| `RefreshToken` | JWT refresh tokens | userId FK, sessionId FK, tokenHash (unique), expiresAt, revokedAt |
| `PasswordResetToken` | Password reset | userId FK, tokenHash, expiresAt, usedAt |
| `EmailVerificationToken` | Email verification | userId FK, tokenHash, expiresAt, usedAt |
| `OAuthAccount` | Social login | userId FK, provider, providerAccountId, accessTokenEncrypted |

### User Preferences (1 model)

| Model | Purpose | Key Fields |
|-------|---------|------------|
| `PreferenceSet` | Reusable preferences | userId FK, name, preferencesJson (JSON) |

### Trip Management (3 models)

| Model | Purpose | Key Fields |
|-------|---------|------------|
| `Trip` | Travel plans | ownerId FK, name, startDate, endDate, status |
| `TripDestination` | Destinations per trip | tripId FK, cityId FK, dayOrder, daysCount |
| `TripPlanningContext` | AI planning context | tripId FK, version, inputsSnapshot (JSON), coverageSnapshot (JSON) |

### Geography (3 models)

| Model | Purpose | Key Fields |
|-------|---------|------------|
| `Country` | Country catalog | isoCode (unique), name |
| `City` | City catalog | countryId FK, name, timezone |
| `CityCoverage` | Coverage level | cityId FK (unique), level, notes |

### Itinerary (4 models)

| Model | Purpose | Key Fields |
|-------|---------|------------|
| `ItineraryVersion` | Versioned itineraries | tripId FK, version, source, status |
| `ItineraryGeneration` | AI generation audit | itineraryVersionId FK, agentName, promptInputsSnapshot (JSON), rationale (JSON) |
| `DayPlan` | Daily itinerary | itineraryVersionId FK, cityId FK, dayDate, status |
| `DailyContext` | Daily context | dayPlanId FK, constraintsSnapshot (JSON), weatherSnapshot (JSON) |

### Activities (3 models)

| Model | Purpose | Key Fields |
|-------|---------|------------|
| `ActivityTypeConfig` | Activity types config | key (PK), displayName, isMandatoryDaily, requiresEvidence |
| `Activity` | Scheduled activities | dayPlanId FK, activityTypeKey FK, type, title, startTime, endTime, sortOrder |
| `ActivityProgress` | Execution status | activityId FK (unique), status, startedAt, completedAt |

### Execution (4 models)

| Model | Purpose | Key Fields |
|-------|---------|------------|
| `StepGuide` | Activity guides | activityId FK, source |
| `StepGuideStep` | Guide steps | stepGuideId FK, stepOrder, title, description |
| `Reminder` | Notifications | activityId FK, type, scheduledAt, status |
| `Incident` | Issue tracking | activityId FK, type, notes |

### Documents (2 models)

| Model | Purpose | Key Fields |
|-------|---------|------------|
| `Document` | Travel documents | tripId FK, type, title, storageProvider, storageKey, checksum |
| `DocumentLink` | Document associations | documentId FK, tripId FK, dayPlanId FK (optional), activityId FK (optional) |

### Playbooks (3 models)

| Model | Purpose | Key Fields |
|-------|---------|------------|
| `PlaybookPack` | Knowledge packs | name, version, metadataJson (JSON) |
| `PlaybookEntry` | Pack entries | playbookPackId FK, key, contentJson (JSON) |
| `LocalAdvicePack` | City-specific advice | cityId FK, playbookPackId FK, status, publishedAt |

### Affiliates (2 models)

| Model | Purpose | Key Fields |
|-------|---------|------------|
| `Recommendation` | Affiliate recommendations | tripId FK, category, title, destinationUrl, displayReason (JSON) |
| `AffiliateClick` | Click tracking | recommendationId FK, clickedAt, userAgent |

---

## Prisma Schema Structure

```prisma
// prisma/schema.prisma

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// ==========================================
// Authentication & Identity
// ==========================================

model User {
  id              String    @id @default(uuid())
  email           String    @unique
  displayName     String
  emailVerifiedAt DateTime?
  status          String    @default("active") // active, disabled
  createdAt       DateTime  @default(now())
  updatedAt       DateTime  @updatedAt

  // Relations
  credential              UserCredential?
  sessions                Session[]
  refreshTokens           RefreshToken[]
  passwordResetTokens     PasswordResetToken[]
  emailVerificationTokens EmailVerificationToken[]
  oauthAccounts           OAuthAccount[]
  preferenceSets          PreferenceSet[]
  trips                   Trip[]

  @@map("users")
}

model UserCredential {
  id                String   @id @default(uuid())
  userId            String   @unique
  passwordHash      String
  passwordAlgo      String   @default("argon2id")
  passwordUpdatedAt DateTime @default(now())

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@map("user_credentials")
}

model Session {
  id          String    @id @default(uuid())
  userId      String
  deviceName  String?
  userAgent   String?
  ipAddress   String?
  createdAt   DateTime  @default(now())
  lastSeenAt  DateTime?
  revokedAt   DateTime?
  expiresAt   DateTime

  user          User           @relation(fields: [userId], references: [id], onDelete: Cascade)
  refreshTokens RefreshToken[]

  @@index([userId, expiresAt(sort: Desc)])
  @@map("sessions")
}

model RefreshToken {
  id        String    @id @default(uuid())
  userId    String
  sessionId String
  tokenHash String    @unique
  createdAt DateTime  @default(now())
  rotatedAt DateTime?
  revokedAt DateTime?
  expiresAt DateTime

  user    User    @relation(fields: [userId], references: [id], onDelete: Cascade)
  session Session @relation(fields: [sessionId], references: [id], onDelete: Cascade)

  @@index([sessionId, expiresAt(sort: Desc)])
  @@map("refresh_tokens")
}

model PasswordResetToken {
  id        String    @id @default(uuid())
  userId    String
  tokenHash String    @unique
  createdAt DateTime  @default(now())
  usedAt    DateTime?
  expiresAt DateTime

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId, createdAt(sort: Desc)])
  @@map("password_reset_tokens")
}

model EmailVerificationToken {
  id        String    @id @default(uuid())
  userId    String
  tokenHash String    @unique
  createdAt DateTime  @default(now())
  usedAt    DateTime?
  expiresAt DateTime

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId, createdAt(sort: Desc)])
  @@map("email_verification_tokens")
}

model OAuthAccount {
  id                    String    @id @default(uuid())
  userId                String
  provider              String // google, apple
  providerAccountId     String
  accessTokenEncrypted  String?
  refreshTokenEncrypted String?
  tokenExpiresAt        DateTime?
  createdAt             DateTime  @default(now())
  updatedAt             DateTime  @updatedAt

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([provider, providerAccountId])
  @@index([userId, provider])
  @@map("oauth_accounts")
}

// ==========================================
// User Preferences
// ==========================================

model PreferenceSet {
  id              String   @id @default(uuid())
  userId          String
  name            String
  preferencesJson Json
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId, createdAt(sort: Desc)])
  @@map("preference_sets")
}

// ==========================================
// Geography
// ==========================================

model Country {
  id        String   @id @default(uuid())
  isoCode   String   @unique
  name      String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  cities City[]

  @@map("countries")
}

model City {
  id        String   @id @default(uuid())
  countryId String
  name      String
  timezone  String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  country          Country           @relation(fields: [countryId], references: [id], onDelete: Cascade)
  coverage         CityCoverage?
  tripDestinations TripDestination[]
  dayPlans         DayPlan[]
  localAdvicePacks LocalAdvicePack[]

  @@unique([countryId, name])
  @@map("cities")
}

model CityCoverage {
  id        String   @id @default(uuid())
  cityId    String   @unique
  level     String // high, medium, low
  notes     String?
  updatedAt DateTime @updatedAt

  city City @relation(fields: [cityId], references: [id], onDelete: Cascade)

  @@map("city_coverages")
}

// ==========================================
// Trip Management
// ==========================================

model Trip {
  id        String   @id @default(uuid())
  ownerId   String
  name      String?
  startDate DateTime
  endDate   DateTime
  status    String   @default("draft") // draft, active, completed
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  owner              User                  @relation(fields: [ownerId], references: [id], onDelete: Cascade)
  destinations       TripDestination[]
  planningContexts   TripPlanningContext[]
  itineraryVersions  ItineraryVersion[]
  documents          Document[]
  documentLinks      DocumentLink[]
  recommendations    Recommendation[]

  @@index([ownerId, status])
  @@index([ownerId, startDate])
  @@map("trips")
}

model TripDestination {
  id        String   @id @default(uuid())
  tripId    String
  cityId    String
  dayOrder  Int
  daysCount Int
  notes     String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  trip Trip @relation(fields: [tripId], references: [id], onDelete: Cascade)
  city City @relation(fields: [cityId], references: [id])

  @@unique([tripId, dayOrder])
  @@map("trip_destinations")
}

model TripPlanningContext {
  id               String   @id @default(uuid())
  tripId           String
  version          Int
  inputsSnapshot   Json
  coverageSnapshot Json
  createdAt        DateTime @default(now())

  trip Trip @relation(fields: [tripId], references: [id], onDelete: Cascade)

  @@index([tripId, version(sort: Desc)])
  @@map("trip_planning_contexts")
}

// ==========================================
// Itinerary
// ==========================================

model ItineraryVersion {
  id        String   @id @default(uuid())
  tripId    String
  version   Int
  source    String // ai, manual, mixed
  status    String   @default("draft") // draft, ready, archived
  createdAt DateTime @default(now())

  trip       Trip                  @relation(fields: [tripId], references: [id], onDelete: Cascade)
  generation ItineraryGeneration?
  dayPlans   DayPlan[]

  @@unique([tripId, version])
  @@map("itinerary_versions")
}

model ItineraryGeneration {
  id                   String   @id @default(uuid())
  itineraryVersionId   String   @unique
  agentName            String
  agentVersion         String
  promptInputsSnapshot Json
  contextInputsUsed    Json?
  citations            Json?
  rationale            Json?
  confidence           Json?
  createdAt            DateTime @default(now())

  itineraryVersion ItineraryVersion @relation(fields: [itineraryVersionId], references: [id], onDelete: Cascade)

  @@map("itinerary_generations")
}

model DayPlan {
  id                 String    @id @default(uuid())
  itineraryVersionId String
  cityId             String
  dayDate            DateTime
  status             String    @default("pending") // pending, ready, in_progress, completed
  refreshedAt        DateTime?

  itineraryVersion ItineraryVersion @relation(fields: [itineraryVersionId], references: [id], onDelete: Cascade)
  city             City             @relation(fields: [cityId], references: [id])
  activities       Activity[]
  dailyContexts    DailyContext[]
  documentLinks    DocumentLink[]
  recommendations  Recommendation[]

  @@unique([itineraryVersionId, dayDate])
  @@index([cityId, dayDate])
  @@map("day_plans")
}

model DailyContext {
  id                  String   @id @default(uuid())
  dayPlanId           String
  constraintsSnapshot Json
  weatherSnapshot     Json?
  createdAt           DateTime @default(now())

  dayPlan DayPlan @relation(fields: [dayPlanId], references: [id], onDelete: Cascade)

  @@index([dayPlanId, createdAt(sort: Desc)])
  @@map("daily_contexts")
}

// ==========================================
// Activities
// ==========================================

model ActivityTypeConfig {
  key              String   @id
  displayName      String
  isMandatoryDaily Boolean  @default(false)
  requiresEvidence Boolean  @default(false)
  allowNotNeeded   Boolean  @default(true)
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt

  activities Activity[]

  @@map("activity_type_configs")
}

model Activity {
  id              String    @id @default(uuid())
  dayPlanId       String
  activityTypeKey String
  type            String // transfer, visit, meal, freeTime, booking, other
  title           String
  locationName    String?
  costEstimate    Decimal?  @db.Decimal(10, 2)
  startTime       DateTime?
  endTime         DateTime?
  sortOrder       Int
  isMandatory     Boolean   @default(false)
  readinessStatus String    @default("pending") // pending, planned, prepared, notNeeded
  readinessNote   String?
  preparedAt      DateTime?

  dayPlan      DayPlan            @relation(fields: [dayPlanId], references: [id], onDelete: Cascade)
  activityType ActivityTypeConfig @relation(fields: [activityTypeKey], references: [key])
  progress     ActivityProgress?
  stepGuide    StepGuide?
  reminders    Reminder[]
  incidents    Incident[]
  documentLinks DocumentLink[]
  recommendations Recommendation[]

  @@unique([dayPlanId, sortOrder])
  @@index([dayPlanId, startTime])
  @@index([dayPlanId, isMandatory, readinessStatus])
  @@map("activities")
}

model ActivityProgress {
  id          String    @id @default(uuid())
  activityId  String    @unique
  status      String    @default("notStarted") // notStarted, inProgress, done, skipped
  startedAt   DateTime?
  completedAt DateTime?

  activity Activity @relation(fields: [activityId], references: [id], onDelete: Cascade)

  @@map("activity_progress")
}

// ==========================================
// Execution
// ==========================================

model StepGuide {
  id         String   @id @default(uuid())
  activityId String   @unique
  source     String // ai, manual, playbook
  createdAt  DateTime @default(now())

  activity Activity        @relation(fields: [activityId], references: [id], onDelete: Cascade)
  steps    StepGuideStep[]

  @@map("step_guides")
}

model StepGuideStep {
  id          String @id @default(uuid())
  stepGuideId String
  stepOrder   Int
  title       String
  description String?

  stepGuide StepGuide @relation(fields: [stepGuideId], references: [id], onDelete: Cascade)

  @@unique([stepGuideId, stepOrder])
  @@map("step_guide_steps")
}

model Reminder {
  id          String   @id @default(uuid())
  activityId  String
  type        String // leaveTime, closingTime, transfer, custom
  scheduledAt DateTime
  status      String   @default("scheduled") // scheduled, sent, cancelled, failed

  activity Activity @relation(fields: [activityId], references: [id], onDelete: Cascade)

  @@index([activityId, scheduledAt])
  @@index([scheduledAt, status])
  @@map("reminders")
}

model Incident {
  id         String   @id @default(uuid())
  activityId String
  type       String
  notes      String?
  createdAt  DateTime @default(now())

  activity Activity @relation(fields: [activityId], references: [id], onDelete: Cascade)

  @@map("incidents")
}

// ==========================================
// Documents
// ==========================================

model Document {
  id              String   @id @default(uuid())
  tripId          String
  type            String // ticket, permit, reservation, insurance, other
  title           String
  storageProvider String // s3, gcs
  storageKey      String
  checksum        String?
  metadataJson    Json?
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  trip  Trip           @relation(fields: [tripId], references: [id], onDelete: Cascade)
  links DocumentLink[]

  @@index([tripId, createdAt(sort: Desc)])
  @@map("documents")
}

model DocumentLink {
  id         String   @id @default(uuid())
  documentId String
  tripId     String
  dayPlanId  String?
  activityId String?
  createdAt  DateTime @default(now())

  document Document  @relation(fields: [documentId], references: [id], onDelete: Cascade)
  trip     Trip      @relation(fields: [tripId], references: [id], onDelete: Cascade)
  dayPlan  DayPlan?  @relation(fields: [dayPlanId], references: [id], onDelete: SetNull)
  activity Activity? @relation(fields: [activityId], references: [id], onDelete: SetNull)

  @@index([tripId, dayPlanId, activityId])
  @@map("document_links")
}

// ==========================================
// Playbooks
// ==========================================

model PlaybookPack {
  id           String   @id @default(uuid())
  name         String
  version      Int
  metadataJson Json?
  createdAt    DateTime @default(now())

  entries         PlaybookEntry[]
  localAdvicePacks LocalAdvicePack[]

  @@map("playbook_packs")
}

model PlaybookEntry {
  id             String @id @default(uuid())
  playbookPackId String
  key            String
  contentJson    Json

  playbookPack PlaybookPack @relation(fields: [playbookPackId], references: [id], onDelete: Cascade)

  @@unique([playbookPackId, key])
  @@map("playbook_entries")
}

model LocalAdvicePack {
  id             String    @id @default(uuid())
  cityId         String
  playbookPackId String
  status         String    @default("draft") // draft, published, archived
  publishedAt    DateTime?

  city         City         @relation(fields: [cityId], references: [id], onDelete: Cascade)
  playbookPack PlaybookPack @relation(fields: [playbookPackId], references: [id], onDelete: Cascade)

  @@index([cityId, status])
  @@index([cityId, publishedAt(sort: Desc)])
  @@map("local_advice_packs")
}

// ==========================================
// Affiliates
// ==========================================

model Recommendation {
  id             String   @id @default(uuid())
  tripId         String
  dayPlanId      String?
  activityId     String?
  category       String
  title          String
  destinationUrl String
  displayReason  Json?
  createdAt      DateTime @default(now())

  trip     Trip      @relation(fields: [tripId], references: [id], onDelete: Cascade)
  dayPlan  DayPlan?  @relation(fields: [dayPlanId], references: [id], onDelete: SetNull)
  activity Activity? @relation(fields: [activityId], references: [id], onDelete: SetNull)
  clicks   AffiliateClick[]

  @@index([tripId, createdAt(sort: Desc)])
  @@map("recommendations")
}

model AffiliateClick {
  id               String   @id @default(uuid())
  recommendationId String
  clickedAt        DateTime @default(now())
  userAgent        String?
  referrer         String?

  recommendation Recommendation @relation(fields: [recommendationId], references: [id], onDelete: Cascade)

  @@index([recommendationId, clickedAt(sort: Desc)])
  @@map("affiliate_clicks")
}
```

---

## Seed Data Requirements

### 1. ActivityTypeConfig (Required - System Configuration)

| key        | displayName      | isMandatoryDaily | requiresEvidence | allowNotNeeded |
|------------|------------------|------------------|------------------|----------------|
| sleep      | Accommodation    | true             | true             | false          |
| transfer   | Transportation   | false            | false            | true           |
| visit      | Visit/Attraction | false            | false            | true           |
| meal       | Meal             | false            | false            | true           |
| free_time  | Free Time        | false            | false            | true           |

### 2. Countries & Cities with Coverage (Required - MVP Destinations)

| Country        | ISO  | City          | Timezone            | Coverage |
|----------------|------|---------------|---------------------|----------|
| France         | FR   | Paris         | Europe/Paris        | high     |
| France         | FR   | Lyon          | Europe/Paris        | medium   |
| Italy          | IT   | Rome          | Europe/Rome         | high     |
| Italy          | IT   | Florence      | Europe/Rome         | high     |
| Italy          | IT   | Venice        | Europe/Rome         | medium   |
| Spain          | ES   | Barcelona     | Europe/Madrid       | high     |
| Spain          | ES   | Madrid        | Europe/Madrid       | high     |
| Japan          | JP   | Tokyo         | Asia/Tokyo          | medium   |
| Japan          | JP   | Kyoto         | Asia/Tokyo          | medium   |
| Japan          | JP   | Osaka         | Asia/Tokyo          | medium   |
| United Kingdom | GB   | London        | Europe/London       | high     |
| United States  | US   | New York      | America/New_York    | high     |
| United States  | US   | San Francisco | America/Los_Angeles | medium   |

**Summary**: 6 countries, 13 cities, 13 coverage records

### 3. Test Users (Development Only)

| Email              | Display Name | Password (plain) | Status |
|--------------------|--------------|------------------|--------|
| test@jaarvi.app    | Test User    | TestPassword123  | active |
| demo@jaarvi.app    | Demo User    | DemoPassword123  | active |
| admin@jaarvi.app   | Admin User   | AdminPassword123 | active |

**Note**: Passwords must be hashed with argon2id before insertion.

### 4. Sample Trips (Development Only)

| Trip Name                | Owner Email      | Status | Destinations                           |
|--------------------------|------------------|--------|----------------------------------------|
| France & Italy Adventure | test@jaarvi.app  | draft  | Paris (5d) → Rome (4d) → Florence (4d) |
| Japan Discovery          | demo@jaarvi.app  | active | Tokyo (7d) → Kyoto (4d) → Osaka (3d)   |

---

## Seed Script Implementation

### Main Seed Orchestrator (`prisma/seed.ts`)

```typescript
import { PrismaClient } from '@prisma/client';
import { seedActivityTypes } from './seeds/activityTypes';
import { seedDestinations } from './seeds/destinations';
import { seedTestUsers } from './seeds/users';
import { seedSampleTrips } from './seeds/sampleTrips';

const prisma = new PrismaClient();

async function main() {
  console.log('🌱 Starting database seeding...\n');
  
  // 1. Activity Types (Required - always run)
  await seedActivityTypes(prisma);
  
  // 2. Destinations (Required - always run)
  await seedDestinations(prisma);
  
  // 3. Test Users (Development only)
  if (process.env.NODE_ENV === 'development') {
    await seedTestUsers(prisma);
    
    // 4. Sample Trips (Development only, requires users)
    await seedSampleTrips(prisma);
  } else {
    console.log('⏭️  Skipping test users and sample trips (not in development mode)\n');
  }
  
  console.log('✅ Database seeding completed successfully!\n');
}

main()
  .catch((e) => {
    console.error('❌ Error during seeding:', e);
    process.exit(1);
  })
  .finally(async () => {
    await prisma.$disconnect();
  });
```

### Activity Types Seed (`prisma/seeds/activityTypes.ts`)

```typescript
import { PrismaClient } from '@prisma/client';

export const activityTypeSeeds = [
  {
    key: 'sleep',
    displayName: 'Accommodation',
    isMandatoryDaily: true,
    requiresEvidence: true,
    allowNotNeeded: false,
  },
  {
    key: 'transfer',
    displayName: 'Transportation',
    isMandatoryDaily: false,
    requiresEvidence: false,
    allowNotNeeded: true,
  },
  {
    key: 'visit',
    displayName: 'Visit/Attraction',
    isMandatoryDaily: false,
    requiresEvidence: false,
    allowNotNeeded: true,
  },
  {
    key: 'meal',
    displayName: 'Meal',
    isMandatoryDaily: false,
    requiresEvidence: false,
    allowNotNeeded: true,
  },
  {
    key: 'free_time',
    displayName: 'Free Time',
    isMandatoryDaily: false,
    requiresEvidence: false,
    allowNotNeeded: true,
  },
];

export async function seedActivityTypes(prisma: PrismaClient): Promise<void> {
  console.log('📝 Seeding activity types...');
  
  for (const type of activityTypeSeeds) {
    await prisma.activityTypeConfig.upsert({
      where: { key: type.key },
      update: type,
      create: type,
    });
    console.log(`  ✓ ${type.displayName} (${type.key})`);
  }
  
  console.log(`  Total: ${activityTypeSeeds.length} activity types\n`);
}
```

### Destinations Seed (`prisma/seeds/destinations.ts`)

```typescript
import { PrismaClient } from '@prisma/client';

export const destinationSeeds = [
  {
    country: { isoCode: 'FR', name: 'France' },
    cities: [
      { name: 'Paris', timezone: 'Europe/Paris', coverage: { level: 'high', notes: 'Full playbooks available' } },
      { name: 'Lyon', timezone: 'Europe/Paris', coverage: { level: 'medium', notes: 'Basic guidance only' } },
    ],
  },
  {
    country: { isoCode: 'IT', name: 'Italy' },
    cities: [
      { name: 'Rome', timezone: 'Europe/Rome', coverage: { level: 'high', notes: 'Full playbooks available' } },
      { name: 'Florence', timezone: 'Europe/Rome', coverage: { level: 'high', notes: 'Full playbooks available' } },
      { name: 'Venice', timezone: 'Europe/Rome', coverage: { level: 'medium', notes: 'Expanding coverage' } },
    ],
  },
  {
    country: { isoCode: 'ES', name: 'Spain' },
    cities: [
      { name: 'Barcelona', timezone: 'Europe/Madrid', coverage: { level: 'high', notes: 'Full playbooks available' } },
      { name: 'Madrid', timezone: 'Europe/Madrid', coverage: { level: 'high', notes: 'Full playbooks available' } },
    ],
  },
  {
    country: { isoCode: 'JP', name: 'Japan' },
    cities: [
      { name: 'Tokyo', timezone: 'Asia/Tokyo', coverage: { level: 'medium', notes: 'Expanding coverage' } },
      { name: 'Kyoto', timezone: 'Asia/Tokyo', coverage: { level: 'medium', notes: 'Expanding coverage' } },
      { name: 'Osaka', timezone: 'Asia/Tokyo', coverage: { level: 'medium', notes: 'Expanding coverage' } },
    ],
  },
  {
    country: { isoCode: 'GB', name: 'United Kingdom' },
    cities: [
      { name: 'London', timezone: 'Europe/London', coverage: { level: 'high', notes: 'Full playbooks available' } },
    ],
  },
  {
    country: { isoCode: 'US', name: 'United States' },
    cities: [
      { name: 'New York', timezone: 'America/New_York', coverage: { level: 'high', notes: 'Full playbooks available' } },
      { name: 'San Francisco', timezone: 'America/Los_Angeles', coverage: { level: 'medium', notes: 'Basic guidance' } },
    ],
  },
];

export async function seedDestinations(prisma: PrismaClient): Promise<void> {
  console.log('🗺️  Seeding destinations...');
  
  let countryCount = 0;
  let cityCount = 0;
  
  for (const dest of destinationSeeds) {
    // Upsert country
    const country = await prisma.country.upsert({
      where: { isoCode: dest.country.isoCode },
      update: { name: dest.country.name },
      create: dest.country,
    });
    countryCount++;
    console.log(`  ✓ ${country.name} (${country.isoCode})`);
    
    // Upsert cities
    for (const cityData of dest.cities) {
      const city = await prisma.city.upsert({
        where: {
          countryId_name: {
            countryId: country.id,
            name: cityData.name,
          },
        },
        update: { timezone: cityData.timezone },
        create: {
          countryId: country.id,
          name: cityData.name,
          timezone: cityData.timezone,
        },
      });
      
      // Upsert coverage
      await prisma.cityCoverage.upsert({
        where: { cityId: city.id },
        update: {
          level: cityData.coverage.level,
          notes: cityData.coverage.notes,
        },
        create: {
          cityId: city.id,
          level: cityData.coverage.level,
          notes: cityData.coverage.notes,
        },
      });
      
      cityCount++;
      console.log(`    • ${cityData.name} (${cityData.coverage.level} coverage)`);
    }
  }
  
  console.log(`  Total: ${countryCount} countries, ${cityCount} cities\n`);
}
```

### Test Users Seed (`prisma/seeds/users.ts`)

```typescript
import { PrismaClient } from '@prisma/client';
import argon2 from 'argon2';

export const testUserSeeds = [
  {
    email: 'test@jaarvi.app',
    displayName: 'Test User',
    password: 'TestPassword123',
  },
  {
    email: 'demo@jaarvi.app',
    displayName: 'Demo User',
    password: 'DemoPassword123',
  },
  {
    email: 'admin@jaarvi.app',
    displayName: 'Admin User',
    password: 'AdminPassword123',
  },
];

export async function seedTestUsers(prisma: PrismaClient): Promise<void> {
  console.log('👥 Seeding test users...');
  
  for (const userData of testUserSeeds) {
    const passwordHash = await argon2.hash(userData.password, {
      type: argon2.argon2id,
      memoryCost: 2 ** 16,
      timeCost: 3,
      parallelism: 1,
    });
    
    const user = await prisma.user.upsert({
      where: { email: userData.email },
      update: {},
      create: {
        email: userData.email,
        displayName: userData.displayName,
        emailVerifiedAt: new Date(),
        status: 'active',
      },
    });
    
    await prisma.userCredential.upsert({
      where: { userId: user.id },
      update: {
        passwordHash,
        passwordAlgo: 'argon2id',
        passwordUpdatedAt: new Date(),
      },
      create: {
        userId: user.id,
        passwordHash,
        passwordAlgo: 'argon2id',
        passwordUpdatedAt: new Date(),
      },
    });
    
    console.log(`  ✓ ${userData.email}`);
  }
  
  console.log(`  Total: ${testUserSeeds.length} test users\n`);
}
```

### Sample Trips Seed (`prisma/seeds/sampleTrips.ts`)

```typescript
import { PrismaClient } from '@prisma/client';

interface TripSeed {
  name: string;
  ownerEmail: string;
  startDate: Date;
  endDate: Date;
  status: string;
  destinations: Array<{
    cityName: string;
    dayOrder: number;
    daysCount: number;
    notes?: string;
  }>;
}

export const sampleTripSeeds: TripSeed[] = [
  {
    name: 'France & Italy Adventure',
    ownerEmail: 'test@jaarvi.app',
    startDate: new Date('2026-03-15'),
    endDate: new Date('2026-03-28'),
    status: 'draft',
    destinations: [
      { cityName: 'Paris', dayOrder: 1, daysCount: 5, notes: 'Explore the City of Light' },
      { cityName: 'Rome', dayOrder: 2, daysCount: 4, notes: 'Ancient history and amazing food' },
      { cityName: 'Florence', dayOrder: 3, daysCount: 4, notes: 'Renaissance art and architecture' },
    ],
  },
  {
    name: 'Japan Discovery',
    ownerEmail: 'demo@jaarvi.app',
    startDate: new Date('2026-04-05'),
    endDate: new Date('2026-04-19'),
    status: 'active',
    destinations: [
      { cityName: 'Tokyo', dayOrder: 1, daysCount: 7, notes: 'Modern metropolis' },
      { cityName: 'Kyoto', dayOrder: 2, daysCount: 4, notes: 'Traditional temples and gardens' },
      { cityName: 'Osaka', dayOrder: 3, daysCount: 3, notes: 'Street food paradise' },
    ],
  },
];

export async function seedSampleTrips(prisma: PrismaClient): Promise<void> {
  console.log('✈️  Seeding sample trips...');
  
  for (const tripData of sampleTripSeeds) {
    // Find owner
    const owner = await prisma.user.findUnique({
      where: { email: tripData.ownerEmail },
    });
    
    if (!owner) {
      console.log(`  ⚠️  Skipping "${tripData.name}" - owner not found: ${tripData.ownerEmail}`);
      continue;
    }
    
    // Check if trip already exists
    const existingTrip = await prisma.trip.findFirst({
      where: {
        ownerId: owner.id,
        name: tripData.name,
      },
    });
    
    if (existingTrip) {
      console.log(`  ⏭️  "${tripData.name}" already exists, skipping`);
      continue;
    }
    
    // Create trip
    const trip = await prisma.trip.create({
      data: {
        ownerId: owner.id,
        name: tripData.name,
        startDate: tripData.startDate,
        endDate: tripData.endDate,
        status: tripData.status,
      },
    });
    
    console.log(`  ✓ ${tripData.name}`);
    
    // Add destinations
    for (const destData of tripData.destinations) {
      const city = await prisma.city.findFirst({
        where: { name: destData.cityName },
      });
      
      if (city) {
        await prisma.tripDestination.create({
          data: {
            tripId: trip.id,
            cityId: city.id,
            dayOrder: destData.dayOrder,
            daysCount: destData.daysCount,
            notes: destData.notes,
          },
        });
        console.log(`    • ${destData.cityName} (${destData.daysCount} days)`);
      }
    }
  }
  
  console.log(`  Total: ${sampleTripSeeds.length} sample trips\n`);
}
```

---

## NPM Scripts (add to package.json)

```json
{
  "scripts": {
    "db:start": "docker-compose up -d",
    "db:stop": "docker-compose down",
    "db:reset": "docker-compose down -v && docker-compose up -d",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "prisma:migrate:create": "prisma migrate dev --create-only",
    "prisma:migrate:deploy": "prisma migrate deploy",
    "prisma:studio": "prisma studio",
    "prisma:format": "prisma format",
    "seed": "ts-node prisma/seed.ts",
    "seed:activity-types": "ts-node -e \"require('./prisma/seeds/activityTypes').seedActivityTypes(require('@prisma/client').PrismaClient())\"",
    "seed:destinations": "ts-node -e \"require('./prisma/seeds/destinations').seedDestinations(require('@prisma/client').PrismaClient())\"",
    "seed:users": "ts-node -e \"require('./prisma/seeds/users').seedTestUsers(require('@prisma/client').PrismaClient())\"",
    "seed:trips": "ts-node -e \"require('./prisma/seeds/sampleTrips').seedSampleTrips(require('@prisma/client').PrismaClient())\""
  }
}
```

---

## Acceptance Criteria

### AC1: Docker & PostgreSQL
- [ ] `docker-compose.yml` is created with PostgreSQL 15 configuration
- [ ] `docker-compose up -d` starts container successfully
- [ ] Database is accessible at `localhost:5432`
- [ ] Health check passes: `pg_isready -U jaarvi_dev -d jaarvi_dev`

### AC2: Prisma Schema
- [ ] `schema.prisma` contains all 27 models
- [ ] All relations are correctly defined
- [ ] All indexes are defined for hot queries
- [ ] `npx prisma validate` passes
- [ ] `npx prisma format` shows no changes needed

### AC3: Migration
- [ ] `npx prisma migrate dev --name init` creates initial migration
- [ ] Migration SQL file is generated in `prisma/migrations/`
- [ ] All tables are created with correct columns and constraints
- [ ] `npx prisma generate` creates client successfully

### AC4: Seed - Activity Types
- [ ] 5 activity types are seeded
- [ ] `sleep` is marked as mandatory daily
- [ ] All types have correct `allowNotNeeded` values
- [ ] Seed is idempotent (running twice doesn't create duplicates)

### AC5: Seed - Destinations
- [ ] 6 countries are seeded
- [ ] 13 cities are seeded with correct country relations
- [ ] 13 city coverage records are created
- [ ] Timezones are correctly set
- [ ] Seed is idempotent

### AC6: Seed - Test Users (Development)
- [ ] 3 test users are created (only in NODE_ENV=development)
- [ ] Passwords are hashed with argon2id
- [ ] Users have `emailVerifiedAt` set
- [ ] UserCredential records are created
- [ ] Seed is idempotent

### AC7: Seed - Sample Trips (Development)
- [ ] 2 sample trips are created
- [ ] Trips are linked to correct owners
- [ ] Trip destinations are created with correct city relations
- [ ] Day orders and counts are correct
- [ ] Seed is idempotent

### AC8: Verification
- [ ] `npx prisma studio` opens and shows all tables
- [ ] All seed data is visible in Prisma Studio
- [ ] Queries work: `SELECT * FROM users`, etc.

---

## Implementation Steps

### Phase 1: Docker Setup
1. Create `docker-compose.yml`
2. Run `docker-compose up -d`
3. Verify: `docker-compose ps` shows healthy container
4. Verify: `psql -h localhost -U jaarvi_dev -d jaarvi_dev` connects

### Phase 2: Prisma Schema
1. Create `prisma/schema.prisma` with all 27 models
2. Run `npx prisma validate`
3. Run `npx prisma format`
4. Run `npx prisma generate`

### Phase 3: Initial Migration
1. Run `npx prisma migrate dev --name init`
2. Review generated SQL in `prisma/migrations/`
3. Verify all tables exist in database

### Phase 4: Seed Scripts
1. Create `prisma/seeds/activityTypes.ts`
2. Create `prisma/seeds/destinations.ts`
3. Create `prisma/seeds/users.ts`
4. Create `prisma/seeds/sampleTrips.ts`
5. Create `prisma/seed.ts` orchestrator

### Phase 5: Run Seeds
1. Set `NODE_ENV=development`
2. Run `npm run seed`
3. Verify with `npx prisma studio`

### Phase 6: Verification
1. Open Prisma Studio
2. Verify all tables have data
3. Test foreign key relations
4. Run sample queries

---

## Verification Queries

```sql
-- Verify activity types
SELECT * FROM activity_type_configs;
-- Expected: 5 rows

-- Verify countries
SELECT * FROM countries;
-- Expected: 6 rows

-- Verify cities with coverage
SELECT c.name, co.name as country, cc.level 
FROM cities c 
JOIN countries co ON c."countryId" = co.id 
JOIN city_coverages cc ON cc."cityId" = c.id;
-- Expected: 13 rows

-- Verify test users
SELECT email, "displayName", status FROM users;
-- Expected: 3 rows (in development)

-- Verify user credentials
SELECT u.email, uc."passwordAlgo" 
FROM users u 
JOIN user_credentials uc ON uc."userId" = u.id;
-- Expected: 3 rows

-- Verify sample trips with destinations
SELECT t.name, u.email as owner, td."dayOrder", c.name as city, td."daysCount"
FROM trips t
JOIN users u ON t."ownerId" = u.id
JOIN trip_destinations td ON td."tripId" = t.id
JOIN cities c ON td."cityId" = c.id
ORDER BY t.name, td."dayOrder";
-- Expected: 6 rows (3 destinations per 2 trips)
```

---

## Definition of Done

- [ ] Docker PostgreSQL container runs successfully
- [ ] Prisma schema has all 27 models with correct relations
- [ ] Initial migration creates all tables
- [ ] All seed scripts execute without errors
- [ ] ActivityTypeConfig has 5 records
- [ ] Country has 6 records
- [ ] City has 13 records
- [ ] CityCoverage has 13 records
- [ ] User has 3 records (development)
- [ ] UserCredential has 3 records (development)
- [ ] Trip has 2 records (development)
- [ ] TripDestination has 6 records (development)
- [ ] Seeds are idempotent (can run multiple times safely)
- [ ] Prisma Studio shows all data correctly
- [ ] Verification queries return expected results

---

## Troubleshooting

| Issue | Solution |
|-------|----------|
| Docker port 5432 in use | Stop other PostgreSQL services or change port in docker-compose.yml |
| Prisma migration fails | Check DATABASE_URL, ensure container is running |
| argon2 install fails | Install build tools: `npm install -g node-gyp` |
| Seed duplicate key error | Use `upsert` instead of `create`, or clean database first |
| Foreign key constraint error | Ensure parent records exist before creating child records |

---

## References

- `ai-specs/specs/backend-standards.mdc` - Database patterns and seed requirements
- `product-discovery/6-DataBase.md` - Complete database schema specification
- `TICKETS/backend-scaffolding.md` - Backend scaffolding (prerequisite)
